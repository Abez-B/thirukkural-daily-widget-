conky.config = {
    -- Window behavior
    own_window = true,
    own_window_type = 'desktop', 
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 216, -- ~85% opacity
    own_window_colour = '1e1e2e', -- Sleek dark theme base
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    -- Geometry
    alignment = 'bottom_right',
    gap_x = 20,
    gap_y = 20,
    minimum_width = 400,
    maximum_width = 400,
    
    -- Rendering
    update_interval = 3600,
    double_buffer = true,
    use_xft = true,
    font = 'TAU-Kabilar:size=14',
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    border_width = 0,
    default_color = 'ffffff',
}

conky.text = [[
${voffset 10}${offset 15}${execpi 3600 python3 /home/abe-z/Projects/kural_widget/kural_flashcard.py}
]]
